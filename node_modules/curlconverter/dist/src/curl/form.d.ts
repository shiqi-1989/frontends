import { Word } from "../shell/Word.js";
import { SrcFormParam } from "../curl/opts.js";
export type FormParam = {
    name: Word;
} & ({
    content: Word;
} | {
    contentFile: Word;
    filename?: Word;
});
export declare function parseForm(form: SrcFormParam[]): FormParam[];
